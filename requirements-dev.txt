# Dev/test dependencies untuk 5min-btc-polymarket.
# Sekali jalan di VPS:  pip install -r requirements-dev.txt
# Memasang paket (editable) + runtime deps + tool dev (mirror pyproject [dev]).
-e .[dev]

# Daftar eksplisit (referensi; sudah tercakup oleh extras [dev] di atas):
pytest>=8.0.0
pytest-asyncio>=0.23.0
respx>=0.21.0
freezegun>=1.4.0
ruff>=0.3.0
black>=24.0.0
mypy>=1.8.0
