# HANDOFF — cepiokta / Polymarket BTC 5-Minute Bot

**Tanggal:** 2026-07-13, Asia/Bangkok  
**Repo:** `marketingcharcoalind-tech/cepiokta`  
**VPS path:** `~/cepiokta`  
**Runtime:** Python 3.11 virtualenv, selalu `source venv/bin/activate`, gunakan `python -m ...`, jangan `uv run`  
**Latest commit sebelum handoff:** `d46c04f` (`style(telegram): apply Black formatting to polling runtime`)  
**Fase:** Phase 2 paper infrastructure selesai secara unit/integration terfokus; operational paper soak belum dimulai  
**G1 directional:** LANJUT KE PAPER, bukan live  
**Pure-arb:** measurement selesai, execution DEFER/STOP  
**Live:** FORBIDDEN

---

## 1. Ringkasan Eksekutif

Proyek adalah bot Polymarket BTC Up/Down 5 menit dengan urutan aman:

```text
readonly data -> backtest/replay -> paper trading -> live micro -> scale
```

Posisi sekarang:

1. G0 readonly sudah lulus dan dataset readonly tetap `orders=0`, `fills=0`.
2. G1 directional sudah memutuskan LANJUT ke paper berdasarkan ALL5/OLD4/NEW dan latency time-based sampai 1000 ms.
3. Pure-arb Prompt 1.7 sudah diukur dan execution dihentikan/defer karena median opportunity 0 ms, maksimum 2 ms.
4. Prompt 2.1 Risk Manager selesai.
5. Prompt 2.2 Paper OMS selesai.
6. Prompt 2.3 Paper runner + ledger selesai sebagai orchestration core dan integration test.
7. Prompt 2.4 reconciliation + critical alert selesai.
8. Telegram T.1 notifier, T.2 read-only commands, dan T.3 confirmed controls selesai secara code/test.
9. Telegram token baru sudah dimasukkan ke `.env` VPS dan diverifikasi valid via Telegram `getMe`; chat ID/whitelist terisi.
10. Telegram polling runtime sudah hidup saat diuji. `/start` dan tombol inline sempat bekerja. UX lalu diubah menjadi persistent reply keyboard yang menempel di bawah.
11. Persistent bottom-menu code dan test sudah dibuat; focused test terakhir sebelum perubahan UX adalah 25/25. Setelah UX persistent menu, test file diperbarui tetapi belum dijalankan ulang di VPS.
12. Paper realtime trading belum di-wire menjadi satu proses penuh dengan Gamma/CLOB WS/Chainlink/PaperRunner/Telegram state bersama. G2 belum lulus.

---

## 2. Aturan Keras untuk AI Penerus

1. User non-teknis. Jangan minta user menulis/edit kode manual.
2. Jika butuh VPS, beri SATU blok command siap copy-paste lalu tunggu output mentah.
3. Jangan mengklaim selesai sebelum output VPS nyata menunjukkan pytest, Ruff, Black, dan mypy lulus untuk scope perubahan.
4. Jangan gunakan `uv run` di VPS.
5. Jangan `git add .`, `git reset --hard`, atau `git clean` di VPS.
6. Working tree VPS punya file lokal, report, CSV, backup, dan modifikasi `src/btcbot/backtest/loss_diagnostics.py`; jangan ditimpa/dihapus sembarangan.
7. Snapshot DB live wajib `VACUUM INTO`, bukan `cp`, lalu `PRAGMA integrity_check`.
8. Jangan hapus `btcbot.db`.
9. Dataset readonly harus tetap orders/fills `0/0`.
10. Phase 2 hanya paper. Jangan membuat signer, CLOB live order, private key, API key, atau transaksi nyata.
11. Jangan ubah strategy, fee, sizing, settlement, label Gamma, atau schema tanpa alasan berbasis data dan approval eksplisit.
12. Semua uang/harga `Decimal`; waktu UTC-aware.
13. Setiap order paper wajib lewat RiskManager.
14. Pure-arb two-leg OMS dilarang untuk sekarang.
15. `.env` VPS adalah runtime config; `.env.example` hanya template tanpa secret.
16. Jangan pernah menampilkan isi `.env` atau token Telegram di output/chat.
17. Token Telegram lama yang pernah masuk repo dianggap bocor dan harus tetap direvoke; token baru hanya ada di VPS `.env`.

---

## 3. Kondisi Directional G1 yang Tetap Berlaku

Kandidat:

```text
t_entry_sec      = 60
delta_threshold  = 50
min_price        = 0.96
max_price        = 0.99
starting_balance = 500
fee              = crypto_fees_v2 7% formula
```

Baseline tick-1:

```text
84 entries
83 wins / 1 loss
Net PnL +$7.40
ROI +1.48%
```

ALL5 time latency:

```text
50 ms   +$7.70
100 ms  +$8.32
250 ms  +$7.88
500 ms  +$7.40
1000 ms +$6.48
```

Kandidat positif pada ALL5, OLD4, dan NEW split. Ini hanya izin paper, bukan live.

Risiko directional tersisa:

- stale target book sekitar 40% pada banyak time variants;
- no-future attempts muncul pada latency 250/500/1000 ms;
- recorder tidak punya full multi-level depth;
- NEW split hanya 349 ronde dan 10–16 entry;
- hanya satu loss, sehingga tail risk belum cukup dipahami;
- book instability warning terlalu banyak false positive untuk langsung dijadikan exit rule.

---

## 4. Pure-Arb Prompt 1.7

Final V3:

```text
rounds processed                    1618
unique paired-book states           14,061,839
valid states                        814
opportunity episodes                462
LVCF duration p25/median/p75/max     0 / 0 / 0 / 2 ms
median best edge                    0.0119
median aggregate-depth upper bound  22,995
theoretical PnL upper bound          $504.462625
```

Keputusan: **DEFER/STOP execution**.

Alasan:

- opportunity terlalu singkat untuk VPS retail;
- two-leg fill belum dimodelkan;
- depth adalah aggregate upper bound, bukan executable best-level depth;
- theoretical PnL bukan profit realistis.

---

## 5. Pekerjaan Phase 2 yang Sudah Selesai

### Prompt 2.1 — Risk Manager

File:

```text
src/btcbot/risk/manager.py
tests/risk/test_manager.py
```

Fitur:

- `Allow | Veto(reason)` fail-closed;
- MAX_NOTIONAL_ROUND;
- MAX_OPEN_EXPOSURE;
- MAX_DAILY_LOSS_PCT;
- MAX_CONSEC_LOSSES;
- MIN_BALANCE/BANKROLL_FLOOR;
- rolling MAX_ORDERS_PER_MIN UTC-aware;
- pause/resume;
- manual dan automatic kill latch;
- circuit breakers: WSS disconnected/reconnecting, stale price, clock drift, abnormal spread, low liquidity, latency breach;
- reconciliation mismatch fatal;
- exit tetap diizinkan saat pause/breaker biasa, tetapi kill fatal memblok semua.

VPS final focused proof:

```text
27/27 tests passed
Ruff passed
Black passed
mypy passed
orders/fills readonly 0/0
```

### Prompt 2.2 — Paper OMS

File:

```text
src/btcbot/exec/oms.py
tests/exec/test_oms.py
```

Fitur:

- hanya dapat start dengan MODE=paper;
- setiap submission lewat RiskManager sebelum book access;
- FOK all-or-nothing;
- FAK partial fill;
- BUY menelusuri asks, SELL menelusuri bids;
- price limit;
- level-walk slippage;
- competition fraction;
- configurable latency;
- idempotency berdasarkan client ID;
- UTC-aware ack/fill;
- GTC ditolak pada paper taker OMS;
- tidak ada CLOB REST, signer, secret, atau live path.

VPS final gabungan sampai 2.2:

```text
41/41 tests passed
Ruff passed
Black passed
mypy passed
orders/fills readonly 0/0
```

### Prompt 2.3 — Paper Runner + Ledger

File:

```text
src/btcbot/app/paper.py
tests/app/test_paper.py
```

Fitur core:

- signal -> strategy -> sizing -> risk -> Paper OMS;
- paper order/fill persistence;
- Decimal cash/position ledger;
- fee `crypto_fees_v2` net-of-fee;
- win/loss settlement memakai Gamma `resolved_outcome`;
- `round_results` dan `equity_curve` mode paper;
- unresolved round fail-closed.

VPS final gabungan sampai 2.3:

```text
46/46 tests passed
Ruff passed
Black passed
mypy passed
orders/fills readonly 0/0
```

Penting: `PaperRunner` adalah orchestration core per tick, belum full operational process yang mengonsumsi adapters live terus-menerus.

### Prompt 2.4 — Reconciliation + Alert

File:

```text
src/btcbot/app/reconcile.py
tests/app/test_reconcile.py
```

Fitur:

- reconcile order -> fill -> position -> Gamma settlement -> PnL -> balance;
- deteksi unknown-order fill, token mismatch, invalid fill, overfill, status-size mismatch, position mismatch, payout mismatch, balance mismatch, PnL mismatch;
- mismatch mengaktifkan `RECONCILIATION_MISMATCH` pada RiskManager;
- mismatch melatch kill-switch;
- emit critical BotEvent ke notifier.

VPS final gabungan sampai 2.4:

```text
61/61 tests passed
Ruff passed
Black passed
mypy passed
orders/fills readonly 0/0
```

---

## 6. Telegram yang Sudah Selesai

### T.1 — Best-Effort Notifier

File:

```text
src/btcbot/adapters/telegram.py
tests/adapters/test_telegram.py
```

Fitur:

- bounded async queue;
- producer non-blocking;
- isolated worker;
- retry;
- Telegram failure tidak menjatuhkan core;
- critical event diprioritaskan di atas info saat queue penuh;
- HTTP transport via existing `httpx`;
- tests memakai MockTransport, tanpa network nyata;
- token tidak dimasukkan ke event/log.

VPS final gabungan sampai T.1:

```text
55/55 tests passed
Ruff passed
Black passed
mypy passed
orders/fills readonly 0/0
```

### T.2 — Read-Only Commands

File:

```text
src/btcbot/app/control.py
tests/app/test_control.py
```

Fitur:

- whitelist;
- `/start`, `/help`;
- `/status`, `/balance`, `/pnl`, `/positions`, `/recent`, `/config`;
- safe config memakai explicit allowlist;
- secret/private/token/API fields tidak pernah ditampilkan;
- safe button definitions;
- unauthorized chats menerima no response.

Focused/gabungan proof sebelum T.3:

```text
72/72 tests passed
Ruff passed
Black passed
mypy passed
orders/fills readonly 0/0
```

### T.3 — Confirmed Risk Controls

File:

```text
src/btcbot/app/control_actions.py
tests/app/test_control_actions.py
```

Fitur:

- `/pause`, `/resume`, `/kill`;
- whitelist;
- wajib konfirmasi dua langkah;
- one-time random token;
- expiry 60 detik;
- cancel;
- anti-replay;
- audit event requested/executed/cancelled/expired;
- memakai RiskManager yang sama;
- tidak bisa mengubah MODE atau risk limits lewat Telegram;
- resume tidak membersihkan breaker aktif;
- kill melatch.

VPS final gabungan:

```text
80/80 tests passed
Ruff passed
Black passed
mypy passed
orders/fills readonly 0/0
```

---

## 7. Telegram Polling Runtime dan UX

File:

```text
src/btcbot/app/telegram_bot.py
tests/app/test_telegram_bot.py
```

Tahap 1 polling runtime:

- `python -m btcbot.app.telegram_bot`;
- long polling Bot API;
- menerima message dan callback query;
- route read-only commands ke T.2;
- route control actions ke T.3;
- whitelist;
- graceful signal handler;
- token tidak di-log.

VPS evidence:

```text
24/24 tests passed pada runtime polling awal
mypy passed
telegram_polling_started allowed_count=1
/start bekerja
inline buttons bekerja
```

Lalu user meminta UX berbeda: tombol harus tetap menempel di bawah, bukan ikut naik bersama chat.

Tahap 2 persistent bottom menu:

- persistent Telegram reply keyboard;
- `resize_keyboard=true`;
- `is_persistent=true`;
- `one_time_keyboard=false`;
- menu:
  - Status
  - P&L
  - Positions
  - Recent
  - Pause
  - Resume
  - Config
  - KILL
- Pause/Resume/KILL tetap memunculkan inline Confirm/Cancel dua langkah.

Focused test code terbaru:

```text
tests/app/test_telegram_bot.py
```

mencakup persistent menu labels, routing tombol Status, KILL hanya menampilkan confirmation, callback read-only, whitelist, dan close.

**Belum diverifikasi setelah perubahan persistent menu:**

- pytest terbaru;
- Ruff;
- Black;
- mypy;
- tes nyata `/start` di Telegram;
- tampilan menu benar-benar menempel di bawah;
- Pause/Resume/KILL confirmation nyata.

Black formatting runtime sudah diterapkan di commit `d46c04f` berdasarkan diff VPS. Test terbaru setelah persistent UX masih harus dijalankan.

Proses polling terakhir sudah dihentikan dengan:

```text
pkill -f "python -m btcbot.app.telegram_bot"
```

Jadi saat handoff dibuat, Telegram bot kemungkinan tidak sedang polling.

---

## 8. Kondisi `.env` VPS

`.env` runtime VPS sudah diganti ke paper config dan divalidasi:

```text
MODE=paper
LIVE_CONFIRMED=no
DB_URL=sqlite+aiosqlite:///./paper.db
T_ENTRY_SEC=60
DELTA_THRESHOLD=50
MIN_PRICE=0.96
MAX_PRICE=0.99
PAPER_STARTING_BALANCE=500
TELEGRAM_ENABLED=true
wallet/private key empty
CLOB credentials empty
```

Validation output yang sudah terlihat:

```text
ENV VALID
mode: paper
live_confirmed: no
db_url: sqlite+aiosqlite:///./paper.db
candidate: 60 50 0.96 0.99
paper_balance: 500
telegram_enabled: True
telegram_whitelist_count: 1
wallet_private_key_set: False
clob_credentials_set: False
```

Telegram token baru:

- dimasukkan tersembunyi langsung di VPS;
- tidak dipaste ke chat;
- `getMe` mengembalikan valid;
- username bot terverifikasi;
- chat ID/whitelist terisi;
- `.env` permission `600`;
- backup `.env` dibuat sebelum perubahan.

Security incident:

- `.env.example` repo sebelumnya ternyata memuat token Telegram nyata dan chat ID;
- token tersebut dianggap bocor;
- `.env.example` sudah dibersihkan pada commit `9eee828`;
- token baru harus dianggap aman hanya jika token lama sudah direvoke lewat BotFather;
- jangan menaruh token baru di `.env.example`, docs, log, commit, atau chat.

Catatan penting:

- `.env.example` adalah template, bukan runtime;
- `.env` VPS adalah runtime config;
- jangan tampilkan `cat .env`;
- paper menggunakan `paper.db`, terpisah dari `btcbot.db` readonly live dataset.

---

## 9. Apa yang Sedang Dikerjakan

Task aktif: **finalisasi Telegram persistent bottom menu**.

Status:

- implementation sudah di GitHub;
- tests sudah diperbarui;
- Black runtime sudah diterapkan;
- belum ada VPS final verification untuk versi persistent menu;
- belum ada live UI verification setelah versi persistent menu;
- polling process sedang berhenti.

Jangan menandai tahap UX ini selesai sebelum verifikasi final.

---

## 10. Langkah Berikutnya, Urutan Wajib

### Step 1 — Verifikasi persistent Telegram menu

Jalankan focused tests + Ruff + Black + mypy untuk:

```text
src/btcbot/app/telegram_bot.py
tests/app/test_telegram_bot.py
src/btcbot/app/control.py
tests/app/test_control.py
src/btcbot/app/control_actions.py
tests/app/test_control_actions.py
```

Lalu start polling dan uji nyata:

- `/start` menampilkan persistent bottom menu;
- tombol tidak ikut naik/hilang setelah message baru;
- Status/P&L/Positions/Recent/Config bekerja;
- Pause menampilkan Confirm/Cancel;
- Cancel tidak pause;
- Pause + Confirm membuat `halted=True`;
- Resume + Confirm menghapus manual pause tetapi tidak breaker;
- KILL + Confirm melatch kill;
- unauthorized chat tidak dapat response.

Setelah tes, hentikan proses. Jangan pakai foreground permanen.

### Step 2 — Buat service/background runtime yang aman

Jangan lagi menjalankan polling foreground yang mengunci terminal. Pilihan sementara: tmux/nohup dengan PID/log; pilihan target: systemd. Namun sebelum background permanen, selesaikan Step 3 agar Telegram dan paper trading memakai runtime state/RiskManager yang sama.

### Step 3 — Wire full operational paper runtime

Saat ini Telegram polling membuat `RuntimeState` dan `RiskManager` sendiri. PaperRunner core juga bisa dibangun terpisah. Ini belum cukup untuk soak paper nyata.

Harus dibuat satu composition root/runtime yang memakai instance bersama:

```text
Gamma discovery
+ CLOB WS books
+ Chainlink price sampling
+ SignalEngine
+ Strategy
+ Sizing
+ satu shared RiskManager
+ PaperOMS
+ PaperLedger/PaperRunner
+ Store paper.db
+ PaperReconciler
+ Telegram notifier/router/actions
```

Critical requirement:

- Telegram Status harus membaca balance/PnL/positions/results paper nyata;
- Pause/Resume/KILL harus memengaruhi RiskManager yang dipakai PaperOMS;
- reconciliation mismatch harus memengaruhi RiskManager yang sama;
- WSS/stale/clock/latency events harus masuk ke breaker shared;
- no duplicate independent risk state;
- Telegram down tidak boleh menghentikan paper core;
- graceful shutdown;
- no live APIs/order path.

### Step 4 — Operational smoke test

Sebelum soak panjang:

- gunakan `paper.db` baru/sehat;
- integrity check;
- start runtime paper beberapa ronde;
- cek paper orders/fills masuk ke `paper.db`, bukan `analisis5.db`/`btcbot.db`;
- cek readonly dataset tetap 0/0;
- cek Telegram PnL/status berubah sesuai paper ledger;
- cek reconciliation bersih;
- cek restart tidak duplicate client IDs/fills;
- cek disk/RAM/log.

### Step 5 — Ratusan ronde paper untuk Gate G2

Ukur:

- total rounds;
- signals;
- orders/fills/rejections;
- FOK/FAK fill rate;
- PnL net fee/slippage;
- equity curve;
- max drawdown;
- consecutive losses;
- stale book incidents;
- no-future/latency failures;
- hedge/exit outcomes;
- reconciliation mismatch count;
- per-round Telegram notifications dan critical alerts.

G2 hanya lulus bila:

- ratusan ronde paper;
- PnL konsisten dengan backtest secara masuk akal;
- zero unresolved reconciliation mismatch;
- risk/control behavior terbukti;
- tidak ada live order/secret path.

### Step 6 — Full repo technical debt cleanup

Full repo baseline sebelumnya menunjukkan:

```text
1315 tests collected
1310 passed
5 failed
```

Kegagalan lama:

1. tiga test `test_delta_sensitivity.py` memanggil API lama `ReplayConfig.from_settings_with_overrides` yang tidak ada;
2. dua test `test_book_snapshots_pk.py` memakai `datetime.replace(second=99)` yang invalid.

Selain itu ada lint/mypy debt besar pada file historis. Ini bukan regresi Prompt 2.1–T.3, tetapi harus dibersihkan sebelum gate besar/CI dianggap benar-benar hijau.

---

## 11. Blocker/Risiko Aktif

| ID | Risiko | Dampak | Tindakan |
|---|---|---|---|
| RT1 | Full paper runtime belum ter-compose | Paper soak belum bisa dimulai jujur | Step 3 di atas |
| TG1 | Persistent menu belum VPS-final-tested | UX belum boleh dianggap selesai | Step 1 |
| TG2 | Polling foreground mengunci terminal | Operasional buruk | shared runtime + systemd/tmux |
| TG3 | Token lama pernah committed | Security | pastikan revoke; token baru hanya VPS |
| TD1 | 5 full-suite tests lama gagal | CI/full gate belum hijau | cleanup terpisah |
| TD2 | lint/mypy debt historis | quality baseline kotor | cleanup bertahap |
| WD1 | VPS working tree berisi local outputs/modifikasi | risiko kehilangan data | jangan reset/clean/add-all |
| B2b | Chainlink Data Streams belum ada | basis risk live | blocker sebelum live |
| B1 | CLOB V2 REST/EIP-712 belum verified | live impossible | jangan sentuh sebelum G2 |
| DATA1 | aggregate depth saja | fill realism terbatas | ukur paper; pertimbangkan full depth |
| G1R1 | NEW split kecil | out-of-sample lemah | ratusan ronde paper |

---

## 12. Status Gate Sekarang

```text
G0 readonly          ✅ LULUS
G1 directional       ✅ LANJUT PAPER
Pure-arb execution   ⏭️ DEFER/STOP
2.1 Risk Manager     ✅
2.2 Paper OMS        ✅
2.3 Paper core       ✅ focused/integration
T.1 Notifier         ✅
2.4 Reconciliation  ✅
T.2 Read-only        ✅
T.3 Controls         ✅
Telegram polling     🟦 works; persistent UX pending final verification
Paper operational    ⬜ not wired
G2 paper validation  ⬜ not started
G3 live               ⛔ FORBIDDEN
```

---

## 13. Commit Map Phase 2/Telegram

```text
44f8875  feat(risk): implement paper-safe risk manager
a30eda8  fix(risk): strict quality
02ea98c  style(risk): final formatting
dd15d52  feat(exec): risk-gated Paper OMS
69208ae  style(exec): Paper OMS final
d3ac14c  feat(app): Paper runner and ledger
86c6342  style(app): paper tests final
7942a72  feat(telegram): best-effort notifier
ab559b0  style(telegram): notifier final
a858848  feat(app): reconciliation and alerts
12b7fdd  style(app): reconciliation final
82e004e  feat(control): Telegram read-only control plane
c0c9f73  test(control): strict tests
5aeaea7  feat(control): confirmed pause/resume/kill
9c23cf1  test(control): final state typing
294bf59  docs: mark T.2/T.3 complete
9eee828  security: remove Telegram credentials from env template
7ad5a31  feat(telegram): polling runtime
89dac0c  feat(telegram): persistent bottom menu
a376576  test(telegram): persistent menu coverage
d46c04f  style(telegram): Black polling runtime
```

---

## 14. VPS Safety Snapshot Terakhir

Repeated verification throughout Phase 2 showed:

```text
analisis5.db orders|0
analisis5.db fills|0
wallet_private_key_set: False
clob_credentials_set: False
MODE=paper
LIVE_CONFIRMED=no
paper DB: paper.db
```

The readonly soak process health was not rechecked after `.env` changed to MODE=paper. Existing long-running processes retain old loaded settings, but any restart will read the new `.env`. Before restarting anything, inspect tmux/processes and do not accidentally replace readonly soak with an incomplete paper runtime.

---

## 15. First Response for the Next AI

The next AI should:

1. Read this handoff fully plus `AGENTS.md`, `PROMPT_GUIDE.md`, `PROGRESS_TRACKER.md`, docs, and specs.
2. Inspect latest commit/diff.
3. Do not restart live/readonly processes blindly.
4. Start with persistent Telegram menu VPS verification.
5. Then design shared operational paper composition root.
6. Keep live forbidden and readonly datasets untouched.
7. Update progress and create a new handoff after operational paper runtime is verified.
